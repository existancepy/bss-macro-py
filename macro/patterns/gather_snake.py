

for i in range(2):
    move.hold("w", 0.72*size)
    move.hold("a", 0.1*width)
    move.hold("s", 0.72*size)
    move.hold("a", 0.1*width)
for i in range(2):
    move.hold("w", 0.72*size)
    move.hold("d", 0.1*width)
    move.hold("s", 0.72*size)
    move.hold("d", 0.1*width)
        
    
    
